# 店铺标识：store1 / store2
STORE_ID=store1

# 本地服务监听端口
PORT=3000

# 数据库文件路径（默认存于项目根 data/ 目录）
DB_PATH=./data/local.db

# 删除记录所需密码
DELETE_PASSWORD=safe@safe

# 第二阶段同步预留（本期未启用）
# CLOUD_SERVER_URL=https://your-oracle-cloud.example.com
# SYNC_INTERVAL_MS=5000
